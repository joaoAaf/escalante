spring:
  application:
    name: escalante-api-test
  datasource:
    url: jdbc:h2:mem:escalante-testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    br:
      com:
        appsemaperreio: DEBUG
    org:
        springframework:
            test: INFO

spring.main:
  allow-bean-definition-overriding: true
env:
  front:
    url: http://localhost:*

  jwt:
    secret: 7cadc739b1a4f4ba8639f78ad73cdf44f5fba0d5e513ced7199b540fd2c1f566
    expiration: 60

  usuario:
    inicial:
      username: "initial-admin"
      password: "adminpass"
    padrao:
      password: "userpass"
